class Member {
  String fullName, role, missionName;
  int age;
  double weight, height;
  bool isCommander;

  Member(this.fullName, this.role, this.age, this.weight, this.height,
      this.isCommander, this.missionName);
}
